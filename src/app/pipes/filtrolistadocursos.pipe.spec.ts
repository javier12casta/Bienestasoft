/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FiltrolistadocursosPipe } from './filtrolistadocursos.pipe';

describe('Pipe: Filtrolistadocursose', () => {
  it('create an instance', () => {
    let pipe = new FiltrolistadocursosPipe();
    expect(pipe).toBeTruthy();
  });
});
