/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FiltrocentrodistribucionPipe } from './filtrocentrodistribucion.pipe';

describe('Pipe: Filtrocentrodistribucione', () => {
  it('create an instance', () => {
    let pipe = new FiltrocentrodistribucionPipe();
    expect(pipe).toBeTruthy();
  });
});
