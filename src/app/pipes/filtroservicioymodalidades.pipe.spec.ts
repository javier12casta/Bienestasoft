/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FiltroservicioymodalidadesPipe } from './filtroservicioymodalidades.pipe';

describe('Pipe: Filtroservicioymodalidadese', () => {
  it('create an instance', () => {
    let pipe = new FiltroservicioymodalidadesPipe();
    expect(pipe).toBeTruthy();
  });
});
