/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FiltrodatosnutricionalesPipe } from './filtrodatosnutricionales.pipe';

describe('Pipe: Filtrodatosnutricionalese', () => {
  it('create an instance', () => {
    let pipe = new FiltrodatosnutricionalesPipe();
    expect(pipe).toBeTruthy();
  });
});
