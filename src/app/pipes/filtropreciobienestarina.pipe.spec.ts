/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FiltropreciobienestarinaPipe } from './filtropreciobienestarina.pipe';

describe('Pipe: Filtropreciobienestarinae', () => {
  it('create an instance', () => {
    let pipe = new FiltropreciobienestarinaPipe();
    expect(pipe).toBeTruthy();
  });
});
